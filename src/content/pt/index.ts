/**
 * Portuguese (pt-BR) dictionary — STUB.
 *
 * For launch this re-exports the English copy as a placeholder, so the EN/PT
 * switch works end-to-end now. To localize, replace this with translated copy:
 * mirror `../en` (e.g. add `../pt/site.ts`, `../pt/about.ts`, …) and build the
 * default bundle the same way. Keys must match the English `Dictionary` shape.
 */
export { default } from "../en";
